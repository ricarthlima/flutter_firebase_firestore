import 'package:flutter/material.dart';

class ListinScreen extends StatelessWidget {
  const ListinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
